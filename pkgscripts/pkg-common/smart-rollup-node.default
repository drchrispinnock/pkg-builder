# Data Directory
# An independent mount point of /var/tezos is recommended
#
DATADIR=/var/tezos/.tezos-smart-rollup-node

# Where the tezos wallet files are stored
CLIENTDIR=/var/tezos/.tezos-client

# Other options to pass the node
#
RUNTIME_OPTS=""
