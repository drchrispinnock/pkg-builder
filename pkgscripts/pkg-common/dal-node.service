[Unit]
Description=Octez-node
Documentation=https://tezos.gitlab.io/
After=network.target

[Service]
Environment="DATADIR=/var/tezos/.tezos-dal-node USER=tezos LOGDIR=/var/log/tezos"
EnvironmentFile=-/etc/default/octez-dal-node
User=$USER
WorkingDirectory=~
Restart=on-failure
RestartSec=10s
KillMode=control-group
TimeoutStartSec=30s
TimeoutStopSec=300s

ExecStart=/usr/bin/octez-dal-node run --data-dir $DATADIR --log-output $LOGDIR/dal-node.log $RUNTIME_OPTS
Slice=octez.slice

[Install]
WantedBy=multi-user.target
